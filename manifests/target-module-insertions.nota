;; Target-specific module insertion index.
;; Each record is: base module id, output surface, inserted module ids.
[
  (skill-editor AgentsSkill [harness-placement])
  (skill-editor ClaudeSkill [harness-placement])
  (management ClaudeSkill [claude-management])
  (management ClaudeAgent [claude-management])
  (general-instructions CodexAgent [codex-skill-loading])
]
